import { theme } from '../index';

// export default (primary, secondary, third) => {
//     if (primary) {
//         theme.primary = primary;
//     }
//     if (secondary) {
//         theme.secondary = secondary;
//     }
//     if (third) {
//         theme.third = third;
//     }
// };

export default (primary, secondary, third) => ({
    primary,
    secondary,
    third
});
