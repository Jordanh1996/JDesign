import { Theme, Consumer } from './createTheme';
import withTheme from './withTheme';
import createTheme from './setTheme';

export const theme = {
    primary: '#0096EF',
    secondary: '#FFFFFF',
    third: '#202020'
};

export { 
    createTheme,
    Theme,
    Consumer,
    withTheme
};

